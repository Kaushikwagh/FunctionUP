import java.util.*;
public class Assignment_07 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
    //Problem 1- String Manipulation
        //Write a Java program that takes a user input string and then performs the following operations: 1. Print the length of the string. 2. Convert the string to uppercase and print it.3. Check if the string contains the word "Java" (case-insensitive) and print "Contains 'Java'" or "Does not contain 'Java'" accordingly.

//        System.out.print("Enter a string: ");
//        String Input = sc.nextLine();
//
//        int len = Input.length();
//        System.out.println("Length of the string: " + len);
//
//        String upperCaseString = Input.toUpperCase();                   //tUC
//        System.out.println("Uppercase string: " + upperCaseString);
//
//        boolean containsJava = Input.toLowerCase().contains("java");
//
//        if (containsJava) {
//            System.out.println("Contains 'Java'");
//        } else {
//            System.out.println("Does not contain 'Java'");
//        }
//        sc.close();



    // Problem 2 - Array Sum and Average
        //Create an array of integers in Java. Calculate and print the sum of all the elements in the array. Then, calculate and print the average of the elements.


//        int [] no = {98, 45, 79, 99, 80};
//        int sum = 0;
//        for (int a : no){
//            sum += a;
//        }
//        float average = (float) sum / no.length;
//        System.out.println("Sum of elements: " + sum);
//        System.out.println("Average of elements: " + average);


        // Problem 3 - Type Casting and Conditional Statements
        //Write a Java program that takes a user input number (a floating-point number) and does the following:
        //If the number is greater than or equal to 10, cast it to an integer and print "Integer part: [integer value]."
        //If the number is less than 10, cast it to an integer and print "Rounded to the nearest integer: [integer value]."
        //These problems will help you practice working with strings, arrays, type casting, conditional statements, and basic user input in Java.

//        System.out.print("Enter a floating-point number: ");
//        float no = sc.nextFloat();
//        if (no >= 10) {
//            System.out.println("Integer part: " + (int) no);
//        } else {
//            System.out.println("Rounded to the nearest integer: " + Math.round(no));
//        }

    }
}
